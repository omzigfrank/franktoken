{
  "name": "franktoken",
  "version": "0.4.3",
  "description": "FrankToken — real-time, cross-device AI session, token, model, and USD cost intelligence across Claude (all surfaces), OpenAI Codex, and ChatGPT, for desktop and the web.",
  "main": "out/main/index.js",
  "author": "Frank Diaz",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "test": "node --test",
    "build": "electron-vite build",
    "hub": "node server/index.js",
    "hub:dev": "electron-vite build && node server/index.js",
    "start": "electron-vite preview",
    "dist": "electron-vite build && electron-builder --config electron-builder.config.cjs",
    "dist:win": "electron-vite build && electron-builder --win --config electron-builder.config.cjs",
    "dist:mac": "electron-vite build && electron-builder --mac --config electron-builder.config.cjs",
    "dist:linux": "electron-vite build && electron-builder --linux --config electron-builder.config.cjs"
  },
  "dependencies": {
    "electron-store": "^10.0.0",
    "protobufjs": "^8.7.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "framer-motion": "^11.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.0",
    "vite": "^5.4.11"
  },
  "build": {
    "appId": "com.frankdiaz.franktoken",
    "productName": "FrankToken",
    "icon": "resources/icon.png",
    "artifactName": "${productName}-${version}-${platform}-${arch}.${ext}",
    "files": [
      "out/**/*",
      "resources/**/*"
    ],
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "FrankToken"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "category": "public.app-category.developer-tools"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Development"
    }
  }
}
